import React, {PropTypes} from 'react'
import ClearCompleted from '../containers/ClearCompleted'

const ListActions = React.createClass({
    render() {
        return (<ClearCompleted/>)
    }
})

export default ListActions
